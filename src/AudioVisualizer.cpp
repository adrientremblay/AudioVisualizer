//
// Created by adrien on 05/02/23.
//

#include "../include/AudioVisualizer.h"

/*
AudioVisualizer::AudioVisualizer(sf::SoundBuffer soundBuffer) : WithFFT(soundBuffer) {
    VA_up.setPrimitiveType(sf::LineStrip);
    VA_down.setPrimitiveType(sf::LinesStrip);
}

void Radio::draw(sf::RenderWindow& window) {
    window.draw(VA_up);
    window.draw(VA_down);
}

void Radio::update() {
    WithFFT::update();
    VA_up.clear();
    VA_down.clear();

    const Vector2f starting_position(0.f, HEIGHT/2.f);
    generate_bars_lr_up(VA_up,starting_position);
    generate_bars_lr_down(VA_down, starting_position);
}
*/
