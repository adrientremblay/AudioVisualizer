//
// Created by adrien on 05/02/23.
//

#ifndef AUDIOVISUALIZER_ABSTRACTMODE_H
#define AUDIOVISUALIZER_ABSTRACTMODE_H


/*
class AbstractMode {
private:
    static AbstractMode* instance;
protected:
    sf::SoundBuffer buffer;

    unsigned int sample_rate = 0;
    sf::Uint64 sample_count = 0;
    sf::Uint64 buffer_size = 0;
public:
    virtual ~AbstractMode() {};
    AbstractMode(SoundBuffer soundBuffer);
    virtual void draw(sf::RenderWindow& window) = 0;
    virtual void update() = 0;
};
*/

#endif //AUDIOVISUALIZER_ABSTRACTMODE_H
